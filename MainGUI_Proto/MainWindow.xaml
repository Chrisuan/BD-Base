<Window x:Class="MainGUI_Proto.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainGUI_Proto"
        mc:Ignorable="d"
        Title="Testablauf" Height="500" Width="800">
    <Grid Height="300" Width="600" VerticalAlignment="Center">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Button Content="Erstes Programm starten" Name="FirstProgrammButton" Click="FirstProgrammButton_Click" Height="20" Width="200" Grid.ColumnSpan="2"/>
        <Label Content="Programm wird ausgeführt" Grid.Row="1" Grid.Column="0"/>
        <Label Content="{Binding Path=IsRunning}" Grid.Row="1" Grid.Column="1"/>


    </Grid>
</Window>
